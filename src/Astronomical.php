<?php

namespace Astronomical;

/**
 * Astronomical Engine contains many symbols and abbreviations. Find below their signification.
 *
 * * `e` Eccentricity (of an orbit)
 * * `h` Altitude above the horizon
 * * `r` Radius vector, or distance of a body to the sun, in Atronomical Unit (AU)
 * * `v` True anomaly
 *
 * * `A` Azimuth
 * * `H` Hour Angle
 * * `M` Mean anomaly
 * * `R` Distance fro Eath to Sun, in Astronomical Unit (AU)
 * * `T` Time in Julian centuries (36525 days) from J2000.0
 *
 * * `α` (alpha) Right ascension
 * * `δ` (delta) Declination
 * * `ε` (epsilon) Obliquity of the ecliptic
 * * `θ` (theta) Sideral time
 * * `π` (pi) Parallax
 * * `τ` (tau) Time in Julian millennia (365250 days) from J2000.0
 * * `ϕ` (phi) Geographical latitude
 * * `ϕ'` (phi prime) Geocentric latitude
 *
 * * `Δ` (delta) Distance to the Earth, in Astronomical Unit (AU)
 * * `ΔT` (delta T) Difference TD - UT
 * * `Δε` (delta espilon) Nutation in obliquity
 * * `Δψ` (delta psi) Nutation in longitude
 *
 * * `AU` Astronomical Unit
 * * `INT` Integer part of a number
 * * `JD` Julian Day
 * * `JDE` Julian Ephemeris Day
 * * `TD` Dynamical Time
 * * `UT` Universal Time
 *
 * * `'` Minutes of a degree  - 1' = one minute of arc  = 1/60th of degree (arcminutes)
 * * `m` Minutes of time      - 1m = one minute of time = 1/60th of an hour
 * * `"` Secondes of a degree - 1" = one second of arc  = 1/3600th of degree (arcseconds)
 * * `s` Seconds of time      - 1s = one second of time = 1/3600th of an hour
 *
 * @package Astronomical
 * @author DUPRE Nicolas
 * @version v0.0.1
 */
class Astronomical
{
}